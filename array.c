#include <stdio.h>

void main() {
	int a,b,c[50],i;
	int sum=0;
	scanf("%d\t%d",&a,&b);
    for(i=0;i<a;i++)
    {
    	scanf("%d",&j[i]);
    }
    for(i=0;i<b;i++)
    {
    	sum=sum+j[i];
    }
    printf("%d",sum);
}
